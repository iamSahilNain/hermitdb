# Build trees (native or via `make build` in the dev container)
build/
build-*/
cmake-build-*/

# Runtime state
data/

# Benchmark raw outputs are reproducible; keep the directory, not the noise
bench/results/*
!bench/results/.gitkeep
!bench/results/FORMAT.md

# Tooling
compile_commands.json
.cache/
.DS_Store
__pycache__/
